import styled from 'styled-components';

export const Header = styled.div`
  text-align: center;
  margin: 0 4rem;

  .title {
    font-size: 3.2rem;
    font-weight: 600;
    line-height: 6.27rem;
  }

  .subtitle {
    font-size: 1.8rem;
    font-weight: 500;
    color: var(--text);
  }
`;
